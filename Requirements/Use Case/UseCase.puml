@startuml
left to right direction
skinparam actorStyle awesome

actor Student
actor Admin

rectangle "LearnMesh Study Group Management System" {

  (Register Account) as UC_Register
  note right of UC_Register
    Creates a new student account using basic credentials.
  end note

  (Login) as UC_Login
  note right of UC_Login
    Authenticates the user and grants access based on role.
  end note

  (Set Learning Preferences) as UC_Preferences
  note right of UC_Preferences
    Allows the student to enter skills, availability, and learning style.
  end note

  (Match Users to Groups) as UC_Match
  note right of UC_Match
    Calculates compatibility between the student and available study groups.
  end note

  (View Group Recommendations) as UC_ViewRecs
  note right of UC_ViewRecs
    Displays ranked study group recommendations generated by matching.
  end note

  (Join Study Group) as UC_Join
  note right of UC_Join
    Enrolls the student into a selected recommended study group.
  end note

  (Create Study Group) as UC_Create
  note right of UC_Create
    Allows a student to create a new study group with initial details.
  end note

  (Monitor System) as UC_Monitor
  note right of UC_Monitor
    Enables the admin to oversee platform activity and system health.
  end note

  (Manage Users) as UC_ManageUsers
  note right of UC_ManageUsers
    Allows the admin to edit, suspend, or remove user accounts.
  end note

  ' Include relationships
  UC_ViewRecs --> UC_Match : <<include>>
  UC_Match --> UC_Preferences : <<include>>
}

' Student interactions
Student --> UC_Register
Student --> UC_Login
Student --> UC_Preferences
Student --> UC_ViewRecs
Student --> UC_Join
Student --> UC_Create

' Admin interactions
Admin --> UC_Login
Admin --> UC_Monitor
Admin --> UC_ManageUsers

@enduml
